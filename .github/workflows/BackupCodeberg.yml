name: Backup Gitea

on:
  push:
    tags:
      - "*"

jobs:
  backuptocodeberg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup git
        run: |
          git config --global user.email "sostaric.borna@hotmail.com"
          git config --global user.name "Shoshta73"
          git remote set-url origin https://git:${CODEBERG_TOKEN}@codeberg.org/Shoshta73/website.git

      - name: push
        run: |
          git push -f --follow-tags origin main
